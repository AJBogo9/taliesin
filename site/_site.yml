# Taliesin marketing site: a website project, built by Taliesin itself.
# Native flat schema (HTML-only; no `project:`/`format:` nesting). The docs are two
# separate book projects; `mounts:` serves them under /docs/guide and /docs/internals
# in `preview` (the static `build` wires each with its own `build ... --out` step).
title: "Taliesin"
description: "A fast, single-purpose dev server that turns one .tmd file into a live blog post, slide deck, book, or website, with executable code and an edit loop that updates the instant you save."
url: "https://taliesin.dev"
favicon: favicon.svg
image: assets/og-card.png   # default social card (og:image / twitter:image) for every page
toc: false

# Serve the two sibling docs books so the nav links resolve live in `preview`.
mounts:
  docs/guide: ../docs/guide
  docs/internals: ../docs/internals

nav:
  left:
    - { text: "Features", href: features.tmd }
    - { text: "Document types", href: formats.tmd }
    - { text: "See it live", href: showcase.tmd }
    - { text: "Guide", href: "docs/guide/" }
    - { text: "Internals", href: "docs/internals/" }
  right:
    - { icon: github, href: "https://github.com/AJBogo9/taliesin" }

footer:
  left:
    - { text: "Built with Taliesin" }
  right:
    - { text: "Guide", href: "docs/guide/" }
    - { text: "Internals", href: "docs/internals/" }
    - { icon: github, href: "https://github.com/AJBogo9/taliesin" }
