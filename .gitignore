# Rust
/target
**/*.rs.bk
*.pdb

# Node / web client tooling
node_modules/
dist/
out/

# Built multi-page sites (qmd-fast build <dir> default output)
_site/
_book/

# Persistent execution cache (rendered cell outputs, keyed by cumulative content
# hash); regenerated on demand, like the build output above.
_freeze/

# Stray media dumped at the repo root by running a post's code cells from here
# (anchored to root so corpus/ .wav fixtures stay tracked). `/figures/` is the
# `#| fig-export:` target from the docs/guide example when built from the repo root.
/*.wav
/figures/

# Logs and env
*.log
.env
.env.local

# OS / editor
.DS_Store
Thumbs.db
.idea/
*.swp

# subagent-driven-development scratch
.superpowers/

# live-edit benchmark runtime artifact (the committed snapshot is RESULTS.md)
tools/live-edit-bench/RESULTS.json
