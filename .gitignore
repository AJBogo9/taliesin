# Rust
/target
**/*.rs.bk
*.pdb

# Node / web client tooling
node_modules/
dist/
out/

# Built multi-page sites (qmd-fast build <dir> default output)
_site/
_book/

# Stray media dumped at the repo root by running a post's code cells from here
# (anchored to root so corpus/ .wav fixtures stay tracked)
/*.wav

# Logs and env
*.log
.env
.env.local

# OS / editor
.DS_Store
Thumbs.db
.idea/
*.swp
