# qmd-fast VS Code extension

The primary client. Hosts a webview preview pointed at the dev server's
websocket and provides the only per-editor glue: translating preview
`goto` messages into `revealRange` + selection in the real editor
(click-to-source).

Scaffolded in **Phase 3**. Empty placeholder for now.
