---
title: "Recipes"
description: "Whole working projects to copy rather than fragments to assemble: front matter, config and layout for two kinds of document."
---

Two complete, copyable setups: a personal blog and a documentation book. Each one shows
the directory layout, the config, and the key front matter, with notes on the parts
that bite. Copy the closest one and edit it down.

Every key used below is real. If you want the full list of what each one does, the
[configuration](../reference/frontmatter.tmd) and [cell options](../reference/cell-options.tmd)
references are the source of truth; this page is the "here is a working whole"
companion to them.

## Books, sites, and the difference

Point either command at a directory and Taliesin builds a project instead of a single
page. Which kind of project depends on one thing: whether that directory's `_site.yml`
lists `chapters:`. If it does, the directory is a **book**, no `type:` key needed; if it
doesn't, it's a website whose pages you compose from front matter, the shape recipe 1
below shows.

The two forms navigate differently, too. A page with `toc: true` gets a table of
contents built from its own headings, a sidebar rail on a wide screen. A book gets no
such rail: its Chapters drawer lists every chapter instead, since what a reader
of a book needs is the neighbouring chapters, not the current one's headings.

The drawer also shows each chapter's length in words, not a reading time, so the cost
of opening a chapter is visible where that decision gets made. It is a word count and
not a reading time on purpose: the count excludes fenced code and math, so a code-heavy
chapter is understated, and a minutes label would carry that error into a promise about
your time that a word count never makes.

The two recipes below are one of each: a blog built from pages and a `listing:`, and a
documentation book built from a `chapters:` list.

## 1. A personal blog

> A home page that lists recent posts, a `blog.tmd` index over a `posts/` folder,
> and an `about.tmd` profile, all generated from front matter.

```text
blog/
├── _site.yml
├── index.tmd            # home: about header + "recent posts" listing
├── blog.tmd             # the full post index (a grid)
├── about.tmd            # a profile page
├── profile.webp
├── favicon.svg
└── posts/
    ├── first-post/
    │   ├── index.tmd
    │   └── thumbnail.webp
    └── second-post/
        └── index.tmd
```

A post is its own folder so its images and any `.bib` live next to it. Page
discovery walks the whole tree, so `posts/first-post/index.tmd` is a real page; the
listing crawls the same folder to build the cards.

The project config, `_site.yml`:

```yaml
title: "Jane Doe"
description: "Notes on things I'm learning"
url: "https://example.com"
favicon: favicon.svg
toc: true
nav:
  - { text: Blog, href: blog.tmd }
  - { text: About, href: about.tmd }
footer:
  left: "© 2026 Jane Doe"
  right:
    - { icon: github, href: "https://github.com/jane" }
```

The home page leads with a `hero:` header and fills a `listing:` with the two most
recent posts. The `id:` makes the listing fill a `::: {#recent}` block where you put
it, instead of appending to the end of the page:

```yaml
---
title: "Jane Doe"
hero:
  headline: "Jane Doe"
  lead: "Notes on things I'm learning."
toc: false
listing:
  id: recent
  contents: posts
  max-items: 3
  type: grid
---

I write about things I am working to understand.

## Recent posts

::: {#recent}
:::

[View all posts →](blog.tmd)
```

The full index, `blog.tmd`, is a listing with nothing else on the page. `page-layout:
full` widens the column so the card grid breathes, and `title-block-style: none`
keeps the title metadata (for the tab and social card) while hiding the big visible
`<h1>`:

```yaml
---
title: "Blog"
title-block-style: none
page-layout: full
listing:
  contents: posts
  type: grid
---
```

Each post supplies what the card shows — title, date, description, image, and a
badge per category — through its own front matter:

```yaml
---
title: "My first post"
description: "What it's about, in one sentence."
date: 2026-05-08
image: "thumbnail.webp"
image-alt: "A scatter plot of the fitted model"
categories: [machine learning, statistics]
---
```

::: {.callout-note}
Newest first is the only order, and there is nothing to configure (the `sort:` key was
retired on 2026-08-02 because every listing that set it wrote the default). A post with
no `date:` still appears, but undated posts cluster deterministically at the end and ties
break on file path, so give every post a date if order matters.
:::

## 2. A documentation book

> Numbered chapters grouped into a part, a Chapters drawer, and Cmd-K search, all
> from one `chapters:` list.

```text
handbook/
├── _site.yml
├── index.tmd            # the cover / preface
├── intro.tmd
├── setup.tmd
├── usage.tmd
└── reference.tmd
```

The presence of `chapters:` in `_site.yml` is what makes the project a book: no
`type:` key needed. Taliesin numbers the chapters, builds the Chapters drawer, wires
prev/next navigation, and resolves cross-references across chapters. A chapter entry
is a bare filename, a `{ part:, chapters: }` group header, or a `{ file:, text: }`
mapping whose `text:` overrides the chapter label:

```yaml
title: "The Handbook"
author: "Jane Doe"
chapters:
  - index.tmd
  - intro.tmd
  - part: "Using it"
    chapters:
      - setup.tmd
      - file: usage.tmd       # { file:, text: } overrides the chapter label
        text: "Everyday use"
  - reference.tmd
```

The Chapters drawer is a book's whole navigation: it lists every chapter, so a book has
no right-hand "on this page" rail (`toc:` is a website key and warns here). The Cmd-K
(Ctrl-K) search palette indexes every chapter. The book output directory defaults to
`_book/` instead of `_site/`. The chapter pages themselves need no special front
matter; a plain `title:` is enough:

```yaml
---
title: "Introduction"
---
```

Without an explicit `text:` override, the chapter label is the chapter's first `#`
heading, then its front-matter `title:`, then the file name. These very docs are a
book built this way: open the Chapters drawer and press Cmd-K to try the search.

::: {.callout-note}
There is no `_metadata.yml` cascade. The root `_site.yml` sets defaults (like
`bibliography:`) and a chapter's own front matter overrides them, two levels, no
directory merge.
:::

## Where to go next

- [Running code](code.tmd): cells, outputs, and the execution cache for any recipe
  with `{python}`.
- [Interactive content](interactive.tmd): live `{js}` cells and reactive inputs to
  add to a post.
- [Configuration reference](../reference/frontmatter.tmd): every per-page `---` key
  the recipes set (`listing:`, `hero:`, `image:`, and the rest) and every `_site.yml`
  key, in full.
- [Cell options reference](../reference/cell-options.tmd): every `#|` cell option,
  including the `label:`/`fig-cap:` keys that number a figure produced by code.
- [CLI reference](../reference/cli.tmd): every command and flag used above.
