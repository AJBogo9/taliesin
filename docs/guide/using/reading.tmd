---
title: "The reader experience"
description: "What a built page gives its reader: theme, reading progress and resume, hover cross-reference cards, anchor links and keyboard access."
---

A Taliesin page is not just rendered HTML, it is a reading surface. Every built
page carries a small suite of reader tools behind a single **Settings** gear in the
top-right corner. They need no author configuration: they appear on every
article automatically (slide decks have their own chrome, so they are skipped).

Two principles hold for everything on this page:

- **Reader-side and read-only.** Each tool stores its state in the *reader's own*
  browser `localStorage`, keyed by the page path. Nothing is written back to your
  `.tmd` source, and nothing leaves the browser. This is the single-editing-surface
  rule applied to the output: the reader owns a reading of the document, the
  document stays authoritative.
- **Exact and durable.** Because every block carries a content-hash
  `data-block-id`, your reading position anchors to block identity, not to fragile
  scroll offsets or fuzzy text. It survives a re-render and a change of window size.

## The Settings menu

The **Settings** gear opens a light-dismiss popover (not a modal: it never covers or
inerts the page). On a website it lives in the navbar; on a book, in the topbar; on a
standalone page it floats in the top-right corner. It hosts Theme, Keyboard shortcuts,
and, on a document that has code cells, Code. Close it with Esc, a click outside, or the
gear again.

## Theme

The **Theme** section is a single segmented control: Light, Dark, or Sepia. The choice
persists in the reader's `localStorage` and is applied *before the page paints*, so a
returning reader never sees a flash of the wrong theme.

The first two are the familiar pair. The third, **Sepia**, is a warm, low-contrast
reading mode: a soft cream page (`#f4ecd8`) with a dark-brown ink (`#5b4636`) instead of
black on white. The point is comfort for long, plain-prose reads, where full Light
contrast can feel harsh, especially in dim light or on a bright panel. It is a true theme,
not a tint: links, code backgrounds, borders, and the focus ring all shift to the sepia
palette together, and it declares `color-scheme: light` so native form controls match.

Sepia is **reader-choice only**. It is not a value you can set in a page's front-matter
`theme:` (that field selects Light, Dark, or an extension bundle): it is a reading mode
the reader picks for themselves, the same way they pick Light or Dark. An author who
wants a warm default ships a custom theme through the usual extension or `css:` route.

There is deliberately no text-size, width, or spacing knob. Taliesin tunes those defaults
once, for every reader, rather than shipping a comfort panel of sliders: the browser
already offers page zoom for the rare reader who needs larger type, and one well-set
measure beats a pile of controls nobody opens.

## Code

On a document with code cells, the Settings menu gains a **Code** section: a segmented
control with Show and Hide. Hide collapses every executed cell's source, leaving the
outputs, figures and tables exactly where they were, so the document reads as prose with
results. Like the theme, the choice is the reader's, persists in their `localStorage`,
and is applied *before the page paints* — a returning reader never watches the code
render and then disappear.

It is the reader's half of a control the author already has. `#| echo: false` is the
author deciding a particular cell's source is not part of the document; this is a reader
deciding, for themselves and for every page they visit, that they want the results and
not the workings. Neither overrides the other in a surprising way: a cell the author
already hid stays hidden, and Show never reveals it.

The section appears **only where it governs something**, and it targets only code *cells*.
A plain ` ```python ` fence is prose you wrote to be read, and a captioned, numbered
listing is a float a reader may have arrived at through an `@lst-` reference; neither is
hidden. A document with no cells never shows the control at all.

## Running it yourself

A page with code cells ends with a quiet box offering **every cell's source as one
script**, one download per language, in the order the cells ran. It needs no server: the
file is carried in the page itself, so a single built `.html` you emailed to someone still
hands them the code.

The part that matters is what it includes. A cell marked `#| echo: false` runs and shows
no listing, and those are usually the imports, the data loading and the plot styling — so a
reader who copied the visible listings by hand would get a script missing its first third
and only find out when it failed. The download is assembled from the document's block
model rather than from the rendered page, so hidden cells are in it.

`{js}` cells are deliberately not offered. A `{js}` document is a reactive graph, not a
script: the runtime orders cells by dependency, not by position, and a cell that reads a
`{{< input >}}` or the page's DOM means nothing outside the page hosting it. Offering that
as a file would be handing the reader something that is not the program that ran. Re-running
a `{js}` document is reloading it.

For the **data** the code reads, see the `{{< dataset >}}` shortcode in
[Writing](writing.tmd): a provenance card stating what a file is, its size, its SHA-256 and
where it came from. A multi-GB parquet does not belong in a folder of HTML, so the
web-native answer to "can I get the data" is a citation you can verify, not a copy.

## Reading progress and resume

For long reads, an unobtrusive pair answers "where was I?", with no control to open.
(There is deliberately no progress bar: the browser's own scrollbar already says how
far down the page you are, and a second copy of that answer is one more thing moving
while you read.)

- A **Resume reading** pill appears when you reopen a page you left partway
  through. It scrolls you back to the exact block you stopped at, anchored to that
  block's content hash. It dismisses itself after a few seconds or on your next
  scroll.
- On pages with a table of contents, sections you have scrolled through gain a
  **read checkmark** in the TOC, so a long reference page shows your trail at a
  glance.

## Hover cross-reference cards

Hovering an internal reference, a `@fig-`/`@sec-`/`@eq-` cross-reference, a `[@cite]`
citation, or a footnote, pops a card showing the *target block itself* (the figure,
the equation, or the bibliography entry). You read
the target without losing your place. The cards render the already-rendered content,
so there is no re-execution, and table-of-contents links are excluded.

## Anchor copy-links

Hovering a heading or a numbered float (figure, table, listing) reveals a small
`#`. Click it to copy the canonical deep link to that anchor. The `#` flashes to a
check on success and announces "Link copied" for screen readers. It works in
insecure contexts (a `--host` LAN preview, `file://`) through a clipboard fallback.

## No focus mode, and no fullscreen

A reading page has neither a focus/reading mode nor a fullscreen toggle, and that is a
decision rather than a gap. Both were shipped and then removed: the chrome a page carries
is a slim topbar or navbar and a footer, all of it static, and static chrome does not
compete for attention the way a moving sidebar or an ad would. Hiding it bought a reader
nothing they could feel, while costing them their navigation and two controls to learn.

A book made the point plainly. Once a chapter's right-rail TOC was removed, focus mode
had literally nothing left to hide there: the sticky topbar was never in its hide list,
so the toggle was a no-op that still occupied a row in the Settings menu.

Fullscreen stays where it earns its place: a **slide deck**, which is presented rather
than read, keeps its own `F` shortcut and toolbar button. For a page, the browser's own
fullscreen (F11) is one key away and belongs to the reader, not to the document.

## Keyboard and accessibility

Tab from the top of any page reveals a **Skip to content** link that jumps focus past
the chrome straight to the prose. The link and its focusable `<main>` target are
emitted server-side, so they work even with JavaScript disabled. A handful of keyboard
shortcuts speed up reading (each is ignored while you are typing in a field or a dialog
is open):

- `?` opens (and closes) the Settings menu, whose Keyboard shortcuts section lists these.
- `/` opens the search palette.
- `←` / `→` move to the previous / next chapter (in a book).
- `Esc` closes the open menu, dialog, or palette.

The two single-character shortcuts (`?`, `/`) can be switched off: the **Shortcuts**
control sits directly above that list in the Settings menu. A bare letter key is easy to fire
by accident, especially with speech input, so WCAG 2.1.4 requires a way to turn character-key
shortcuts off, and this is it. Switching them off hides the list (the keys are dead, so
advertising them would be a lie) and leaves the Settings gear as your way back in. `Esc` and
the arrow keys are not character keys, so they always stay live. The choice persists per
reader, in their own `localStorage`.

This is not a contradiction of the no-comfort-panel stance above: a text-size slider is a
preference, whereas a shortcut you cannot turn off is a barrier.

Keyboard focus is always visible: every interactive control (links, buttons, tabs,
the Settings controls, and the deck controls) shares one consistent focus ring,
shown only for keyboard or assistive-technology focus and never on a mouse click. The
ring is a single theme token, so it follows the active theme (including Sepia and the
high-contrast modes below).

**Landmarks and dialogs.** Each `<nav>` on a page carries a distinguishing accessible
name (the table of contents announces as "Table of contents", separate from the
navbar and the prev/next pager), so a screen reader's landmark list is navigable.
Modal overlays (the search palette and the image viewer) move focus into themselves on
open, trap Tab while open, and restore focus to whatever opened them on close. The
**Settings** menu is a light-dismiss popover: it moves focus to its first control on open and
returns it to the gear when you press Esc, but it never traps Tab, so you can always leave it
by tabbing or by clicking away. (It moves focus because the panel is not adjacent to its gear
in the page's source order, so without the move a keyboard reader would be stranded far from
the menu they just opened.)

**Slide decks** announce as you navigate. Each slide is a labelled group ("Slide 3 of
12"), and a polite live region speaks the new position (and its title) on every move,
so a deck is navigable and announceable with a screen reader.

**High contrast.** In Windows High Contrast / `forced-colors` mode, and under
`prefers-contrast: more`, borders, focus rings, separators, and the active-item
markers are re-asserted with system colors so the chrome stays legible when the
palette is forced.

Prose also gets quiet typographic polish that needs no configuration: balanced
headings and captions, pretty last-line wrapping, and figures that keep their caption
attached.
