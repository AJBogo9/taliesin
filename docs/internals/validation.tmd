---
title: "Validation & schemas"
description: "Diagnostics that are purely advisory and always located: the closed vocabularies, did-you-mean, the prose linter, and check and lsp."
---

Mistype `#| eccho: false` in `post.tmd`'s cell, or write `treme:` for `theme:` in its
front matter, and Taliesin does two things at once: it **renders the document anyway**,
and it drops a **located warning** in the dev panel that you can click straight to the
offending line. That pairing, render-anyway plus a precise, jumpable diagnostic, is the
whole design of the validation layer.

## The invariant: purely diagnostic, always located

Every validator in Taliesin obeys one rule: it is **purely diagnostic**. An
unrecognized key, kind, or option never changes what renders; the block emits exactly
as it would have, and the validator's only effect is to append a `Warning`. A typo
costs you a note, not a broken page.

A `Warning` is a small struct (`message`, `file`, `line`), and the `file` + `line` are
the point: they ride the same source map the block model uses, so a warning about an
included partial names the partial and the original line. The dev panel renders each
warning as a click-to-source link, the same Ctrl-click bridge the preview uses (see [the
block model](block-model.tmd)). A diagnostic you can't locate is noise; a located one is
a fix.

## The closed vocabularies

Taliesin validates against its **own** closed sets of keys, defined independently
in `frontmatter.rs`, `render/validate.rs`, and `site/config/`. There are five
surfaces, each a list-membership check:

| Surface | Checked against | Where |
|---|---|---|
| Front-matter top-level keys | `KNOWN_KEYS` | `frontmatter.rs` |
| Nested `execute:` / `listing:` / `hero:` children | `EXECUTE_KEYS` / `LISTING_KEYS` / `HERO_KEYS` | `frontmatter.rs` |
| Code-cell `#|` / `//|` / `%%|` options | `CELL_OPTION_KEYS` | `render/validate.rs` (called from the pipeline) |
| Callout kinds (`.callout-<kind>`) | `CALLOUT_KINDS` | `render/validate.rs` (called from `divs.rs`) |
| `_site.yml` keys | `NATIVE_KEYS` | `site/config/` |

Two `:::` containers are also checked for **structure** rather than vocabulary: a
`.panel-tabset` with no headings (it would render no tabs) and a `.code-walkthrough`
with no code block (its sticky panel would be empty) each warn, since the author almost
certainly meant something.

`format:` sub-keys used to be the one deliberate hole, exempted "because a format
extension may introduce its own". There is no such mechanism — `_extensions/` is a
theme-CSS lookup and nothing else — so the exemption was certifying inert keys as
supported on a green check. They are linted now, and the sub-key that *appeared* to work
(`format:` → `html:` → `toc:`) only did so because the TOC scan matched at any depth;
that scan is top-level-only now, so every `format:` sub-key is uniformly inert and the
warning is true.

## Did you mean?

Membership tells you a key is wrong; a good diagnostic tells you what you meant. Every
surface shares one helper, `unknown_key_message`, which appends a suggestion when a
known key is within **Levenshtein edit distance 2** of the typo (`closest()`):

```text
unknown front-matter key `treme` (did you mean `theme`?)
unknown cell option `eccho` (did you mean `echo`?)
unknown _site.yml key `favicn` (did you mean `favicon`?)
```

Distance 2 is the sweet spot: it catches the realistic slips (a swapped, dropped, or
doubled letter) without guessing wildly at a key that is simply absent from Taliesin's
vocabulary. When nothing is close enough, the message is just the bare "unknown" form,
no misleading suggestion.

### Suggesting a name Taliesin does not own

Keys come from a **closed** vocabulary, so `closest()` takes a `&[&'static str]`. A
broken reference is the opposite case: the candidates are names *the author* invented,
read at run time, so `cite::nearest()` matches over owned strings instead. Renaming a
label is the commonest way an author silently breaks their own document, so both halves
of the reference syntax suggest a target:

```text
broken cross-reference: @fig-reslts (did you mean `@fig-results`?)
broken citation: @bishop2006patern (did you mean `@bishop2006pattern`?)
```

The two halves reach the candidates differently. A citation is warned by `cite::render`,
where the parsed bibliography is already in hand. A cross-reference is warned later, by
`cite::validate_xrefs`, which runs after a site has had its chance to resolve the anchor
on another page; by then the render-time anchor registry is gone, so the vocabulary is
read back off the rendered HTML (each target still carries its `id="fig-results"`).

Two guards keep the suggestions honest. A suggestion must share the broken reference's
**kind prefix**, because a `@fig-` that resolved to a section would render the label
"Figure" over a link to prose. And matching runs on the *stem* after that prefix, subject
to a five-character floor: below it, a distance-2 edit rewrites most of the name, and
`@fig-a` would cheerfully "suggest" `@fig-b`. Suggestions are scoped to the page's own
anchors, which is also all that is left to suggest from.

## The prose linter (opt-in)

The vocabulary checks above are always on; the prose linter (`prose.rs`) is the one
validator a document **opts into**, since prose rules are a matter of taste. It stays off
unless the front matter sets a `prose-lint:` key: `prose-lint: true` enables the built-in
rules, and a mapping form additionally reads a custom `banned:` string list
(`prose-lint:` with `banned: [utilize, leverage]`). Any other value (`false`, null, a
scalar) leaves it off.

It is markdown-aware and, like every other validator, **purely diagnostic**: it returns
`(line, message)` pairs that render maps onto the same located, click-to-source channel
as a broken xref or an unknown key. The messages carry no prefix; they read like
"repeated word `we`", "weasel word `very` (consider cutting)", or "banned term
`utilize`". Three high-precision rules ship:

- **doubled words** (`the the`),
- a fixed list of **weasel words** (`very`, `really`, `just`, `actually`, `basically`,
  `simply`, `clearly`, `obviously`, ...), and
- the document's own **banned-terms** list.

All three are whole-word and case-insensitive. The scanner first strips everything that
is not prose, front matter, fenced code blocks, `:::` fence lines, inline code, math,
link/image targets, autolinks, and HTML tags, so only running prose is checked (a `very`
inside `$...$` or a banned term in a code span never fires).

Because the linter scans the fully-expanded source (after `{{< include >}}` resolution),
a hit inside an included partial is attributed back to that partial's file and line, the
same source-map the block model uses. Only the **top-level** document's `prose-lint:` key
controls whether it runs, though: an included partial can't turn the linter on or off.
The warnings flow through the normal `doc.warnings` channel, so they appear in both the
live preview's dev panel and `taliesin check`.

## The clean break: no legacy-tolerance tier

A key that is unknown because it is a legacy term Taliesin does not implement is
reported exactly like a typo: as unknown. This is a deliberate stance, not an oversight.
Taliesin is its own tool with its own closed vocabulary, so there is no silent
"tolerate but ignore" tier for unknown keys, the single most confusing failure mode of a
compatibility shim, where a setting appears to be accepted but quietly does nothing.
The corpus was purged of legacy-only keys to hold this line.

## Schemas: the same vocabulary, one step earlier

Validation at render time is the backstop. The **earlier** you learn a key is wrong, the
better, so Taliesin also ships JSON Schemas that let your editor's YAML language server
flag the same mistakes as you type, before you ever render.

```{mermaid}
%%| fig-cap: "One source of truth (the closed-set consts) feeds both the render-time validator and the bundled JSON Schemas."
flowchart LR
  consts["closed-set consts<br/>KNOWN_KEYS · EXECUTE/ABOUT/HERO · NATIVE_KEYS"]
  consts --> val["render-time validator<br/>(located warnings)"]
  consts --> bless["bless test<br/>(TALIESIN_BLESS=1)"]
  bless --> schema["bundled Draft-2020-12 schemas<br/>FRONTMATTER_SCHEMA · SITE_SCHEMA"]
  schema --> cli["taliesin schema --out .tmd"]
  cli --> lsp["editor YAML language server"]
```

The two schemas (`FRONTMATTER_SCHEMA`, `SITE_SCHEMA` in `schema.rs`) are committed as
static strings, but they are **generated from the same closed-set consts** the validator
uses, and **regenerated only** through a bless test (`TALIESIN_BLESS=1 cargo test -p
taliesin-core --lib schema`). They are never hand-edited, so they **cannot drift** from
what the renderer enforces: the editor and the renderer always agree on the vocabulary.
A second test asserts each schema's structural shape, so a malformed bless can't slip
through.

`taliesin schema [--out <dir>]` writes both files out, and a one-line modeline points the
language server at them:

```yaml
# yaml-language-server: $schema=.tmd/tali-site.schema.json
title: My site
```

This is the in-scope on-ramp to editor **autocomplete**: schema-driven front-matter
completion needs **no Taliesin-specific language server**, just two schema files and the
YAML support the editor already has. (Live **diagnostics** are a separate story:
`taliesin lsp`, [below](#taliesin-lsp-live-diagnostics-in-any-editor), streams them to any
LSP editor.) The authoring side is covered in the [Configuration
reference](../guide/reference/configuration.html); the config validator's role in a
multi-page project is in [the multi-page subsystem](sites.tmd).

## `taliesin check`: the static preflight superset

`taliesin check <file|dir>` renders the document **in memory**, with **no code
execution**, lists every located diagnostic (greppable `path:line: message` lines, or
`--format json` for a CI gate), and exits non-zero if any are found. It is a strict
*superset* of what preview and build surface: it collects `doc.warnings` (every validator
above) **and** a family of static document lints that only `check` runs, all on the same
click-to-source channel. The lints live in `crates/core/src/diagnostics/` and are read-only static
analysis over the rendered block model, plus the cross-page checks in the site layer:

| Check | Function | What it catches |
|---|---|---|
| Cross-references | `cite::validate_xrefs` | a `@fig-`/`@sec-`/... pointing at no label |
| Duplicate heading ids | `validate_duplicate_heading_ids` | two headings emitting the same explicit `{#id}` |
| In-page anchors | `validate_internal_anchors` | a `[text](#anchor)` matching no element id |
| Local images | `validate_local_assets` | an `<img src>` whose file is missing under the doc dir |
| Local video | `validate_local_media` | a `{{< video clip.mp4 >}}` / `<video src>`/`poster=` whose file is missing |
| Local links | `validate_local_links` | a relative `[text](other.tmd)` link whose target file (or its `.tmd` source) is missing |
| Reactive graph | `validate_js_reactive_graph` | a `{js}` cell's `//\| input:` naming nothing defined, or a dependency cycle |
| Accessibility | `validate_a11y` | a heading-level skip, an `<a>`/`<button>` with no accessible name, an `<img>` with no `alt` |
| Code languages | `validate_code_languages` | a fence whose language resolves to no syntax, so it silently renders unhighlighted (a typo like ` ```pyton `). Tokens that are plain on purpose (`text`, `txt`, `plain`, `console`, `output`, `none`) never warn |
| Math | `validate_math` | a `$…$` / `$$…$$` expression KaTeX cannot parse |
| Citations | `citations_without_bibliography` | references present but no `bibliography:` declared, by the page **or** its project |

Each lint is conservative by design, so a green `check` is a no-false-positive promise.
Two patterns recur: anything a static (no-execution) pass can't resolve is **skipped**
rather than guessed at, so the internal-anchor and dangling-input checks bail out for any
document with executable cells (a `{python}`/`{r}` cell can emit an id, or a `define`
can publish a name, at runtime); and image-only asset checks skip `<audio>`/streamed
media a static pass can't verify. A `.html` link whose `.tmd` source exists on disk is
accepted (the site build will emit the page), so an intra-project link is never
false-flagged. External links are never fetched: `check` stays offline and deterministic.

A site `check` runs the same per-page lints over every discovered page, then adds the
cross-page link + anchor existence check (`Site::validate_cross_page_links`), which a
single-doc `check` can't do because only the page registry knows each `.tmd`'s real
output URL. A directory that has a `_quarto.yml` but no `_site.yml` gets the generic
"no `_site.yml`" diagnostic (no special migration breadcrumb). Using `check` in
practice is covered in the guide's [CLI reference](../guide/reference/cli.html).

## `taliesin lsp`: live diagnostics in any editor

`check` is the batch gate; `taliesin lsp` is the same validators, live. It runs an
offline **Language Server Protocol** server over stdio, so an LSP editor (Neovim, Helix
and VS Code today; Zed needs a published extension to register a new server at all) gets
`.tmd` diagnostics **as you type**, on the unsaved buffer, before you save or render.
Syntax highlighting is not part of that: there is no tree-sitter grammar, and the server
answers `textDocument/semanticTokens` with "method not found" — the guide's
[CLI reference](../guide/reference/cli.html) says so where a reader setting this up will
meet it. It is parse-only: **no kernel, no code execution**, and
**read-only** (the single-editing-surface invariant holds, the server never writes to
your source).

The server reuses the exact seam `check` uses. `check::buffer_diagnostics` lints an
editor buffer in memory, a thin wrapper over `collect_file_diagnostics_from_src`; the
LSP server calls it on every edit, then projects each `check::Diagnostic` to an LSP one with `Diagnostic::to_lsp`
(1-based line/column → 0-based range; a precise `[col, end_col)` span when the validator
located a token, else the whole line; `severity`, the stable `TAL-*` `code`, and a
`codeDescription` link to the diagnostics catalog). So the squiggles are byte-for-byte
the same findings `check` and the render path emit, one source of truth.

```{mermaid}
%%| fig-cap: "One buffer-linting seam feeds the batch gate and the LSP server alike."
flowchart LR
  seam["check::buffer_diagnostics<br/>(in-memory render + validators)"]
  seam --> cli["taliesin check<br/>(the batch gate, from disk)"]
  seam --> lsp["taliesin lsp<br/>(didOpen / didChange → publishDiagnostics)"]
  lsp --> ed["Neovim · Helix · Zed · VS Code"]
```

The protocol surface is deliberately one capability: `textDocumentSync` is **full-text**
(the whole buffer arrives on every change, matching the whole-buffer lint), and the
server advertises nothing else yet. It tracks open documents by URI, admits only
`languageId: taliesin`, and clears a document's squiggles on `didClose`. Editor wiring is
one line, e.g. Neovim `vim.lsp.start({ cmd = { "taliesin", "lsp" } })`. Hover,
go-to-definition, completion, and outline are additive capabilities that will layer onto
this same server; today they live in the VS Code companion's own providers.
