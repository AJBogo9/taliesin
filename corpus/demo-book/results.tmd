# Results

What we found, building on the methods in @sec-methods (in particular the
configuration described in @sec-setup). It also leans on the three stages in
@fig-pipeline from the methods chapter.

```{mermaid}
%%| label: fig-stages
%%| fig-cap: "How a single run moves through the three estimation stages."
flowchart LR
  Fit --> Check
  Check --> Report
```

@fig-stages summarises the path one run takes through the pipeline.

## Discussion

A short discussion of the findings.
