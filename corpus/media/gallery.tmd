---
title: "An image gallery"
---

A multi-image page becomes a gallery with `layout-ncol`: a row of figures, each still a
normal, numbered, captioned, cross-referenceable figure in its own right.

::: {layout-ncol=3}
![No pooling.](fit-b.png){#fig-no-pooling}

![Complete pooling.](fit-c.png){#fig-complete-pooling}

![Hierarchical (partial pooling).](fit-a.png){#fig-hierarchical}
:::

The three fits above (@fig-no-pooling, @fig-complete-pooling, and @fig-hierarchical) sit
side by side, in the order they were written, and each keeps its own caption and number.

## Side-by-side columns

`{layout-ncol=N}` (used above) is the column grid, and the only spelling: a split lays
out side by side rather than silently stacking. Each direct child block is one column, so
there is no per-column fence to write and nothing to keep in step with the count. Widths
are equal by construction:

::: {layout-ncol=2}
The left column holds one idea.

The right column holds the other, beside it.
:::
