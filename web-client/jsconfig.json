{
  "compilerOptions": {
    "checkJs": true,
    "strict": true,
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "noEmit": true,
    "types": [],
    "skipLibCheck": true
  },
  "//": "Globbed, not enumerated: a hand-written list silently leaves a NEW file unchecked, which is the one failure this gate exists to prevent. Every .js in this directory is a preview-client script.",
  "include": ["*.js", "*.d.ts"]
}
